{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the app windows (main + the stash grid window)",
  "windows": ["main", "stash"],
  "permissions": [
    "core:default",
    "opener:default",
    "notification:default"
  ]
}
